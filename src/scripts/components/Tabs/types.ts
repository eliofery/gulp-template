export type Selectors = {
  root: string
  button: string
  content: string
}

export type StateAttributes = {
  ariaSelected: string
  tabIndex: string
}

export type State = {
  activeTabIndex: number
}
